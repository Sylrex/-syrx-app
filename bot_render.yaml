services:
  - type: web
    name: syrx-bot
    env: python
    plan: free
    buildCommand: pip install -r bot_requirements.txt
    startCommand: python bot.py
    repo: https://github.com/your-repo/syrx-app
    branch: main
    envVars:
      - key: PYTHON_VERSION
        value: 3.8
